rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser app never talks to Firestore directly.
    // All reads/writes must go through the REST API, which verifies Firebase Auth ID tokens.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
